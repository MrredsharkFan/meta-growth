var app = new Vue({
    el: "#app",
    data: {
        player,
        format,

        mul,
        exp,
        log_exp,
        get_gain,

        cost_bu1,
        cost_bu2,
        cost_bu3
    }
}
)